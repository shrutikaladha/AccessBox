<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.accessbox">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <protected-broadcast android:name="android.intent.action.MEDIA_MOUNTED" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_access_box"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.HomePageActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AccessBoxTheme"></activity>
        <activity
            android:name=".activity.SelectPhotosActivity"
            android:label="@string/app_name"
            android:theme="@style/AccessBoxTheme" />
        <activity
            android:name=".activity.GalleryViewActivity"
            android:label="@string/app_name"
            android:theme="@style/AccessBoxTheme" />
        <activity
            android:name=".activity.BaseActivity"
            android:label="@string/app_name"
            android:theme="@style/AccessBoxTheme" />
        <activity
            android:name=".activity.SubCategoryActivity"
            android:label="@string/app_name"
            android:theme="@style/AccessBoxTheme" />
        <activity
            android:name=".activity.SplashActivity"
            android:label="@string/app_name"
            android:theme="@style/AccessBoxTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
